# The log file name and location can be set in
# /etc/my.cnf by setting the "log-error" option
# in either [mysqld] or [mysqld_safe] section as
# follows:
#
# [mysqld]
# log-error=/opt/anaconda1anaconda2anaconda3/data/mysqld.log
#
# In case the root user has a password, then you
# have to create a /root/.my.cnf configuration file
# with the following content:
#
# [mysqladmin]
# password = <secret> 
# user= root
#
# where "<secret>" is the password. 
#
# ATTENTION: The /root/.my.cnf file should be readable
# _ONLY_ by root !

/opt/anaconda1anaconda2anaconda3/data/mysqld.log {
        # create 600 mysql mysql
        notifempty
        daily
        rotate 5
        missingok
        compress
    postrotate
	# just if mysqld is really running
	if test -x /opt/anaconda1anaconda2anaconda3/bin/mysqladmin && \
	   /opt/anaconda1anaconda2anaconda3/bin/mysqladmin ping &>/dev/null
	then
	   /opt/anaconda1anaconda2anaconda3/bin/mysqladmin flush-logs
	fi
    endscript
}
